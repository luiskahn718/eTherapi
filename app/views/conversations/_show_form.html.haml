= simple_form_for :message, url: [:reply, conversation] do |f|
  = f.input :body
  .form-actions
    = f.button :submit, class: 'btn-primary'
    = submit_tag 'Cancel', type: :back, class: 'btn btn-danger'